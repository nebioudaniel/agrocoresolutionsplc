import type { Metadata } from 'next'
import { Inter, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const inter = Inter({ 
  subsets: ["latin"],
  variable: '--font-inter',
})

const geistMono = Geist_Mono({ 
  subsets: ["latin"],
  variable: '--font-geist-mono',
})

export const metadata: Metadata = {
  title: 'Agrocore Solutions PLC | Agricultural Consulting Excellence',
  description: 'Specialized consulting services in agrochemical regulation, agricultural development, and environmental management in Ethiopia and beyond.',
  keywords: ['agriculture', 'consulting', 'Ethiopia', 'agrochemical', 'regulatory', 'environmental'],
  authors: [{ name: 'Agrocore Solutions PLC' }],
  icons: {
    icon: '/favicon.svg',
    apple: '/apple-icon.png',
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="bg-background">
      <body className={`${inter.variable} ${geistMono.variable} font-sans antialiased`}>
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
